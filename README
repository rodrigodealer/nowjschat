# pre - requisites (install using  npm install <module>)
- jasmine-node
- mongoose
- now
- should
# type in project directory:
# $ npm install


# init model
var inst = new UserModel();

# saving model
inst.name = "Rodrigo";
inst.login = "dealer";
inst.password = "123";
inst.save();